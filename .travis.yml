language: python
python:
    - "3.6"
before_install:
    - pip install pytest pytest-cov coveralls
    - pip install -r requirements.txt
script:
    - coverage run setup.py test
after_success:
    - coveralls